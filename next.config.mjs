/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn.dummyjson.com", "firebasestorage.googleapis.com", "ads.rozilshrestha.com.np", "localhost"],
  },
};

export default nextConfig;
